# 동물 과녁 맞추기 게임 - 최종 기획서

어린이 대상, 밝고 재미있는 분위기의 브라우저 과녁 맞추기 게임.
단일 `index.html` 파일로 동작 (HTML + CSS + JS, 외부 라이브러리/서버 없음).

## 1. 화면 흐름

```
포인터 선택 화면 ──┬─→ 옵션 화면 (과녁 이미지 / 시작·종료음 / 총소리·파괴음 / 소리 크기 / 마우스 감도 지정)
                  ├─→ 게임 방법 화면 (규칙 설명)
                  └─→ "⚡ 민첩도 테스트" (아래 별도 분기)
        │
        ▼ "놀이 시작!"
   3-2-1-START! 카운트다운
        │
        ▼
   게임 플레이 화면 (30초, 5x7 과녁판, 동시 최대 15개 과녁, 포인터 화면 밖 이탈 방지)
        │
        ▼ (시간 종료 또는 "게임 종료" 버튼 → 포인터 고정 자동 해제)
   결과 화면 (최종 점수 + 순위표 + 재도전/종료)
        │
        └─→ "재도전" 또는 "종료" → 포인터 선택 화면으로 리셋

포인터 선택 화면 ──▶ "⚡ 민첩도 테스트" ──▶ 3-2-1-START! 카운트다운 ──▶ 민첩도 테스트 플레이 화면
   (게임 플레이 화면과 동일한 화면을 재사용, 과녁 1개만 랜덤 위치에 등장)
        │
        ▼ (시간 종료 또는 "게임 종료" 버튼 → 포인터 고정 자동 해제)
   민첩도 테스트 결과 화면 (최종 점수 + 명중 수 + 전용 순위표 + 재도전/종료)
        │
        └─→ "재도전" 또는 "종료" → 포인터 선택 화면으로 리셋
```

## 2. 화면별 기능

### 2-1. 포인터 선택 화면 (시작 화면)
- 조준선(크로스헤어) 모양 4종 중 선택: 원+점 / 원+십자 / 십자선 / 이중원(도넛)
- 선택한 스타일은 `localStorage`에 저장되어 다음에 열어도 유지됨
- 버튼: **⚙️ 옵션**, **놀이 시작! ▶**, **📖 게임 방법**, **⚡ 민첩도 테스트**

### 2-2. 옵션 화면
- **과녁 이미지 지정**: 폴더 선택(`webkitdirectory`) 또는 파일 여러 개 직접 선택 두 가지 방식 모두 지원 → 이미지 파일을 읽어 IndexedDB에 영구 저장. 지정하지 않으면 기본 동물 이미지 사용
- **시작음 / 종료음 지정**: 파일 하나 선택 또는 폴더 선택(폴더 안 소리 파일 중 매번 랜덤 재생) 두 가지 방식 모두 지원 → IndexedDB에 저장. 지정하지 않으면 Web Audio API로 생성한 기본 비프음 사용
- **총소리 / 과녁 부서지는 소리 지정 (신규)**: 게임 중 클릭(발사) 시 총소리, 명중 시 파괴음이 재생됨. 각각 파일 하나 선택 또는 폴더 선택(랜덤 재생) 두 가지 방식 지원 → IndexedDB에 저장. 지정하지 않으면 Web Audio API로 합성한 기본 총소리/파괴음 사용
- **소리 크기 (신규)**: 슬라이더(0~100%)로 게임의 모든 소리(시작·종료음, 총소리, 파괴음, 기본 비프음 포함) 볼륨을 한 번에 조절. `localStorage`에 저장되어 다음에 열어도 유지됨
- **마우스 감도 (신규)**: 슬라이더(0.2x~3.0x)로 게임 중 마우스 이동량에 곱해지는 감도를 조절. `localStorage`에 저장되어 다음에 열어도 유지됨
- 각 항목에 "기본값으로 초기화" 버튼 제공
- "저장하고 돌아가기" → 포인터 선택 화면으로 복귀

### 2-3. 게임 방법 화면
- "옵션" 버튼 아래 **📖 게임 방법** 버튼으로 진입
- 게임 규칙을 9단계로 안내 (8단계까지는 기존 놀이 방법, 9단계는 민첩도 테스트 안내)
- 하단 **닫기** 버튼 → 포인터 선택 화면으로 복귀

### 2-4. 카운트다운
- 화면 정중앙에 **3 → 2 → 1 → START!** 순서로 표시 (1초 간격, 확대 애니메이션)
- 시작 시점에 시작음 재생

### 2-5. 게임 플레이 화면
- **5행 x 7열 = 35칸** 그리드, 화면 크기에 맞춰 반응형으로 표시
- 35칸 모두 항상 활성: 각 칸이 독립적으로 "대기 → 랜덤 등장 → 랜덤 지속시간 후 자동 소멸(못 맞히면 무점) → 재대기" 사이클 반복
- 단, 화면에 동시에 표시되는 과녁은 **최대 15개**로 제한됨. 15개가 이미 떠 있으면 다른 칸은 자리가 빌 때까지 잠시 대기했다가 등장
- 과녁은 매번 랜덤 크기로 등장하며, **크기·지속시간·점수가 하나의 공식으로 연동**:
  - 작을수록: 점수 높음(최대 100점), 지속시간 짧음(최소 0.5초)
  - 클수록: 점수 낮음(최소 10점), 지속시간 김(최대 3초)
- 과녁 그림은 사용자 지정 이미지가 있으면 그중 랜덤, 없으면 기본 동물(강아지/고양이/토끼/곰/병아리) 중 랜덤
- 좌클릭으로 명중: 과녁이 3x3 조각으로 쪼개져 사방으로 튀며 사라지는 "깨지는 효과" + "+점수" 텍스트가 떠오르며 사라짐 + 총점 합산
- 빈 곳 클릭: 감점 없음, 아무 반응 없음
- 상단 HUD: 현재 총점, 남은 시간(30초 카운트다운)
- 화면 하단 **⏹ 게임 종료** 버튼: 30초를 다 채우지 않아도 언제든 눌러서 즉시 결과 화면으로 이동
- **이 화면에서만** 마우스 포인터가 조준선 커서로 바뀌고, 다른 화면에서는 평범한 포인터 사용
- **마우스 감도 조절 & 포인터 화면 이탈 방지 (신규)**: 게임 화면에 들어가는 순간 포인터 락(Pointer Lock)이 걸려 실제 OS 커서는 움직이지 않고, 옵션에서 설정한 감도가 곱해진 가상 커서만 화면 안에서 이동함 → 마우스가 아무리 빠르게 움직여도 게임 화면 밖으로 나가지 않음. 게임 종료(시간 종료/⏹ 게임 종료 버튼) 즉시 포인터 락이 자동 해제되어 평범한 마우스로 복귀

### 2-6. 결과 화면
- 화면 정중앙에 **최종 점수**를 큰 글씨로 표시 + 종료음 재생
- 닉네임을 입력하고 "저장하기"를 누르면:
  - 로컬 순위표(상위 10개, `localStorage`)에 이름+점수 기록
  - 몇 등인지 안내 문구 표시 (10위 밖이면 "10등 안에는 들지 못했어요" 안내)
  - 1~10위 순위표 표시 (이번 기록은 강조 표시)
- 버튼: **🔄 재도전** (즉시 포인터 선택 화면으로), **🏁 종료** (인사 토스트 후 포인터 선택 화면으로)

### 2-7. 민첩도 테스트 플레이 화면 (신규 미니게임)
- 포인터 선택 화면의 **⚡ 민첩도 테스트** 버튼으로 진입, 동일한 3-2-1-START! 카운트다운 후 시작
- 게임 플레이 화면(5x7 그리드, HUD, ⏹ 게임 종료 버튼)을 그대로 재사용하되, **한 번에 과녁 1개만** 랜덤한 칸에 등장
- 진행 순서: 대기(0.35~0.9초) → **화살표 예고**(0.45~0.95초, 과녁이 나올 칸 위에 화살표 표시) → **과녁 등장**(최대 0.2초 동안만 노출, 그 안에 못 맞히면 랜덤 타이밍에 자동으로 사라짐) → 다시 대기
- **시안성**: 화살표가 뜨는 동안 화살표 주변(과녁이 등장할 칸)만 밝은 원형으로 남기고 나머지 화면은 어둡게 처리되는 스포트라이트 효과 적용
- **과녁이 나오기 전(화살표만 있는 단계)에는 클릭해도 총이 발사되지 않음** — 조준만 가능하고, 과녁이 실제로 나타나야만 사격 가능
- 명중 시 점수는 **반응 속도가 빠를수록 높음**(최대 100점, 최소 20점), 기존 게임과 동일한 "깨지는 효과" + "+점수" 텍스트 재사용
- 30초 동안 진행하며, 상단 HUD에 점수와 남은 시간 표시(기존 게임 플레이 화면과 공통 UI)
- 마우스 감도 조절 및 포인터 화면 이탈 방지는 기존 게임 플레이와 동일하게 적용됨

### 2-8. 민첩도 테스트 결과 화면 (신규)
- 최종 점수와 "명중 수 / 전체 시도 수"를 함께 표시
- 닉네임 입력 후 저장하면 **일반 게임과는 별도인 전용 순위표**(상위 10개)에 기록 (일반 모드 점수와 섞이지 않음)
- 버튼: **🔄 재도전**, **🏁 종료** (일반 결과 화면과 동일한 동작으로 포인터 선택 화면으로 복귀)

## 3. 자료 저장 방식

| 자료 | 저장 위치 | 우선순위 |
|---|---|---|
| 포인터 스타일, 일반 모드 순위표(top 10) | `localStorage` (`pointerStyle`, `rankings`) | - |
| 민첩도 테스트 전용 순위표(top 10, 신규) | `localStorage` (`rankings_agility`) | - |
| 소리 크기 / 마우스 감도 (신규) | `localStorage` (`masterVolume`, `mouseSensitivity`) | - |
| 사용자가 옵션에서 고른 과녁 이미지/시작음/종료음/총소리/파괴음(신규) | `IndexedDB` (경로 무관 영구 보관, 폴더 지정 시 여러 개 배열로 저장 후 재생마다 랜덤 선택) | 1순위 |
| 파일에 내장된 기본 자료(`EMBEDDED_CUSTOM_IMAGES` 등) | `index.html` 소스 코드 내부 | 2순위 (IndexedDB에 없을 때) |
| 게임 기본 내장 동물 그림 / 기본 비프음 / 기본 총소리·파괴음(신규, 합성음) | 코드 내 인라인 SVG / Web Audio 합성 | 3순위 (최종 fallback) |

- IndexedDB에 저장된 자료는 원본 폴더/파일이 삭제·이동되어도 계속 사용 가능
- `index.html` 안에는 현재 실제로 동물 그림 9개와 시작음/종료음이 base64로 내장되어 있어, 이 파일 하나만 다른 PC로 옮겨도(IndexedDB가 없는 새 브라우저라도) 같은 그림/소리가 그대로 재생됨
- 내장된 동물 그림 9개는 원본의 흰색 배경을 제거해 투명 PNG로 처리되어 있어, 어떤 칸 배경색 위에서도 자연스럽게 보임
- (과거에는 옵션 화면에 이 내장 자료를 갱신하는 "내장해서 저장하기" 버튼이 있었으나, 더 이상 필요하지 않아 제거함. 자료를 새로 바꾸고 싶으면 옵션에서 폴더/파일을 다시 선택하면 IndexedDB에 저장되어 그대로 사용됨)

## 4. 폴더/파일 구조
- `index.html` — 게임 전체 (HTML/CSS/JS 인라인, 기본 동물 이미지·사운드 내장)
- `targets/` — 사용자가 과녁으로 쓸 그림을 넣어두는 폴더 (옵션 화면에서 직접 선택), `README.txt`에 사용법 안내

## 5. 디자인 톤
- 밝고 화사한 색상(파스텔+비비드: 노랑/하늘/핑크/민트/보라), 크고 둥근 글씨
- 버튼/카드 둥근 모서리 + 그림자, 클릭 시 살짝 눌리는 애니메이션
- 카운트다운/명중/결과 점수 등장 시 통통 튀는 스케일 애니메이션

## 6. 알려진 제약
- `webkitdirectory` 폴더 선택과 파일 선택 대화상자는 브라우저 표준 동작으로, 자동화 테스트로는 다이얼로그 내부까지 확인하지 못함(직접 클릭해 정상 동작 확인 완료된 기능)
- 순위표는 브라우저(로컬)에 저장되므로 다른 기기와 순위를 공유하지 않음
- 과녁 이미지 표시 크기는 각 칸의 실제 렌더링 크기를 기준으로 계산되므로, 창 크기를 게임 중 크게 바꾸면 이미 떠 있는 과녁의 비율이 약간 달라질 수 있음(다음 등장부터 자동 보정)
- **포인터 락(마우스 화면 이탈 방지, 신규)**: 브라우저 표준 Pointer Lock API를 사용하므로, 잠기는 순간 브라우저가 "Esc 키를 누르면 포인터 잠금이 해제됩니다" 같은 안내를 잠깐 보여줄 수 있음(브라우저 기본 동작이며 게임 오류 아님). 플레이어가 Esc를 누르면 브라우저가 자동으로 잠금을 풀어주며, 이 경우 게임은 자연스럽게 일반 마우스 추적으로 되돌아가 계속 플레이할 수 있음. 일부 브라우저/환경(예: 자동화 도구, 창이 포커스를 잃은 상태)에서는 포인터 락 요청이 거부될 수 있으며, 이 경우에도 게임은 오류 없이 일반 마우스 추적으로 동작함

## 7. 버그 수정 내역

### 7-1. 과녁 빠른 더블클릭 시 멈추는 현상 (수정 완료)
- **증상**: 과녁을 빠르게 더블클릭하면 과녁이 멈춘 것처럼 반응하지 않는 문제
- **원인**: 과녁으로 쓰는 `<img>` 요소가 브라우저 기본값으로 드래그 가능한 상태였음. 더블클릭 시 두 클릭 사이 마우스가 미세하게 움직이면 브라우저가 이를 "이미지 드래그 시작"으로 인식해 정상적인 클릭 처리를 방해함
- **조치**:
  - 과녁 이미지에 `draggable=false` 설정, `dragstart`/`mousedown` 기본 동작 차단
  - CSS에 `user-drag:none`, `user-select:none` 계열 속성 추가로 크로스 브라우저 드래그·선택 방지
- **확인**: 빠른 더블클릭을 시뮬레이션해 첫 클릭은 정상 채점·제거되고, 두 번째 클릭은 빈 칸에 떨어져 아무 영향이 없음을 확인. 이후에도 과녁판이 멈추지 않고 정상적으로 계속 동작함

## 8. 추가 기능 구현 노트 (마우스 감도 / 민첩도 테스트 / 옵션 확장)

기존 코드는 한 줄도 수정하지 않고, 같은 `<script>` 안 맨 끝부분에 새 코드만 추가하는 방식으로 구현됨. 핵심 동작 원리는 다음과 같음.

### 8-1. 마우스 감도 조절 & 포인터 화면 이탈 방지
- 게임 화면("놀이 시작!" 또는 "⚡ 민첩도 테스트" 클릭 시) `document.body.requestPointerLock()`을 호출해 OS 커서를 화면에 고정시키고, 이후 `mousemove`의 `movementX/Y`(상대 이동량)에 감도 값을 곱해 화면 범위 안에서만 움직이는 가상 커서 좌표를 계산함
- 포인터 락 중에는 실제 클릭 좌표가 항상 잠금 시점 위치로 고정되어 버리므로, 문서 캡처 단계에서 원래 클릭을 가로챈 뒤 가상 커서 좌표(`document.elementFromPoint`) 기준으로 클릭을 다시 발생시켜, 기존 과녁 클릭 판정 로직이 그대로 정확히 동작하도록 함
- `body.in-game` 클래스가 사라지는 시점(시간 종료, 게임 종료 버튼 등 기존 코드의 화면 전환)을 `MutationObserver`로 감지해 자동으로 포인터 락을 해제함

### 8-2. 민첩도 테스트
- 기존 `cells`/`board`/`buildBoard()`/`shatterTarget()`/`showFloatingScore()` 등을 그대로 재사용하되, 일반 모드의 `gameRunning`과는 별개인 `agilityRunning` 상태로 독립 진행되어 서로 간섭하지 않음
- 화살표+스포트라이트는 CSS `box-shadow: 0 0 0 9999px rgba(0,0,0,0.82)` 기법으로 구현(작은 원 바깥 전체를 어둡게 채우는 방식)

### 8-3. 옵션 확장 (총소리 / 파괴음 / 볼륨)
- 기존 `playSound`/`playBeep` 함수는 선언부를 그대로 둔 채, 스크립트 끝에서 같은 변수에 볼륨 반영 버전을 재대입(오버라이드)하여 소리 크기 조절이 기존 시작·종료음에도 함께 적용되도록 함
- 총소리/파괴음은 사용자가 지정하지 않으면 Web Audio로 합성한 소리(총소리: 짧은 화이트노이즈, 파괴음: 하강하는 사각파)를 기본값으로 사용
- 폴더로 지정한 소리/이미지는 배열로 저장되어 재생·사용 시마다 그중 하나가 랜덤으로 선택됨
